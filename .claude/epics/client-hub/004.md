---
name: Tags, Notes, Tasks API
status: closed
created: 2026-01-12T04:48:34Z
updated: 2026-01-15T07:31:08Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
---

# Task: Tags, Notes, Tasks API

## Description

Implement API endpoints for client tags, notes, and tasks management including the agent task dashboard.

## Checklist

**Tags API**:
- [x] Implement POST /api/clients/:id/tags (add tag)
- [x] Implement DELETE /api/clients/:id/tags/:tagId (remove tag)
- [x] Add tag autocomplete endpoint for existing tags

**Notes API**:
- [x] Implement POST /api/clients/:id/notes (create note)
- [x] Implement PATCH /api/clients/:id/notes/:noteId (update note)
- [x] Implement DELETE /api/clients/:id/notes/:noteId (delete note)
- [x] Add is_important filter support

**Tasks API**:
- [x] Implement POST /api/clients/:id/tasks (create task)
- [x] Implement PATCH /api/clients/:id/tasks/:taskId (update task)
- [x] Implement DELETE /api/clients/:id/tasks/:taskId (delete task)
- [x] Implement GET /api/tasks (agent task dashboard)
- [x] Add completed_at auto-set when status=completed
- [x] Add filtering by status, priority, due date range

- [x] Tests written (TDD: before implementation, or after)
- [x] All tests passing
- [x] Code reviewed

## Reference

See epic.md sections:
- "API Endpoints" for all request/response specifications
- "Database Schema" for table structures and constraints

## Dependencies

- Task 001 (Database Schema) must complete first
- Can run in parallel with Task 003 (Client CRUD API)

## Effort: Medium (6 hours)

## Completion Summary

Implemented using TDD workflow (RED-GREEN-REFACTOR) with 52 new unit tests:
- TagService: 15 tests
- NoteService: 15 tests
- TaskService: 22 tests

All 119 service tests passing, no lint or TypeScript errors.
