---
name: Tags, Notes, Tasks API
status: open
created: 2026-01-12T04:48:34Z
updated: 2026-01-12T04:48:34Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
---

# Task: Tags, Notes, Tasks API

## Description

Implement API endpoints for client tags, notes, and tasks management including the agent task dashboard.

## Checklist

**Tags API**:
- [ ] Implement POST /api/clients/:id/tags (add tag)
- [ ] Implement DELETE /api/clients/:id/tags/:tagId (remove tag)
- [ ] Add tag autocomplete endpoint for existing tags

**Notes API**:
- [ ] Implement POST /api/clients/:id/notes (create note)
- [ ] Implement PATCH /api/clients/:id/notes/:noteId (update note)
- [ ] Implement DELETE /api/clients/:id/notes/:noteId (delete note)
- [ ] Add is_important filter support

**Tasks API**:
- [ ] Implement POST /api/clients/:id/tasks (create task)
- [ ] Implement PATCH /api/clients/:id/tasks/:taskId (update task)
- [ ] Implement DELETE /api/clients/:id/tasks/:taskId (delete task)
- [ ] Implement GET /api/tasks (agent task dashboard)
- [ ] Add completed_at auto-set when status=completed
- [ ] Add filtering by status, priority, due date range

- [ ] Tests written (TDD: before implementation, or after)
- [ ] All tests passing
- [ ] Code reviewed

## Reference

See epic.md sections:
- "API Endpoints" for all request/response specifications
- "Database Schema" for table structures and constraints

## Dependencies

- Task 001 (Database Schema) must complete first
- Can run in parallel with Task 003 (Client CRUD API)

## Effort: Medium (6 hours)
